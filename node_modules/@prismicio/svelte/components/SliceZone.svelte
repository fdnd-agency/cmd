<script>import TodoSliceComponent from './TodoSliceComponent.svelte';
export let slices = [];
export let components = {};
export let context = {};
export let defaultComponent = undefined;
export let dev = false;
</script>

{#each slices as slice, index}
  {@const type = "slice_type" in slice ? slice.slice_type : slice.type }
  {@const Component = components[type] || defaultComponent}
  {#if Component}
    <svelte:component this={Component} {slice} {slices} {context} {index} />
  {:else}
    <TodoSliceComponent {slice} {dev} />
  {/if}
{/each}
