import { hasContext, getContext } from "svelte";
import { prismicKey } from "./injectionSymbols";
import "./types";
export const usePrismic = () => {
    if (hasContext(prismicKey)) {
        return getContext(prismicKey);
    }
    else {
        return {};
    }
};
