import * as prismic from "@prismicio/client";
import * as prismicH from "@prismicio/helpers";
const configurePrismic = (globalOptions) => {
    const { repositoryName, htmlSerializer, linkResolver, slices } = globalOptions;
    const endpoint = prismic.getEndpoint(repositoryName);
    const client = prismic.createClient(endpoint, globalOptions);
    const clientWithFetch = () => prismic.createClient(endpoint, { ...globalOptions, fetch });
    return {
        ...prismicH,
        client,
        clientWithFetch,
        htmlSerializer,
        linkResolver,
        slices,
    };
};
export default configurePrismic;
